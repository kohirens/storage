name: quality-control
run-name: ${{ github.actor }} is running QA
on:
  push:
    branches-ignore: [ main,  auto-update-changelog ]

concurrency:
  group: qa-group
  cancel-in-progress: true

env:
  S3_BUCKET_NAME: ${{ vars.S3_BUCKET_NAME }}
  S3_BUCKET_NAME_BI_PROD: ${{ vars.S3_BUCKET_NAME_BI_PROD }}
  S3_BUCKET_NAME_BI_TEST: ${{ vars.S3_BUCKET_NAME_BI_TEST }}
  VERBOSITY_LEVEL: ${{ vars.VERBOSITY_LEVEL }}

jobs:
    build-n-test:
        runs-on: ubuntu-latest
        name: qa
        steps:
            - name: "fetch code"
              uses: actions/checkout@v4
            - uses: actions/setup-go@v5
              with:
                check-latest: true
                cache-dependency-path: |
                  go.sum
            - name: configure aws credentials
              uses: aws-actions/configure-aws-credentials@v4.1.0
              with:
                role-to-assume: ${{ secrets.AWS_IAM_ROLE }}
                role-session-name: cicd-aws-session
                aws-region: ${{ vars.AWS_REGION }}
            - name: "Run backend test"
              uses: ./.github/actions/test-backend
