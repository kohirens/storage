# Guide to Contributing

We'll keep it simple.

* Keep the code idiomatic.
* Provide documentation and examples.
* Packages that extend this library go in the `pkg` folder. Use the vendor name
as the package name. For example, the S3 bucket package that extends this
library to allows use of S3 as storage is stored in `pkg/aws` directory in this
repository.
